[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# SPA Fallback routing for React Router DOM
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
